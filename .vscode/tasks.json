{
  "tasks": [
    {
      "type": "shell",
      "label": "clang++-4.0 build active file",
      "command": "/usr/local/bin/clang++-4.0",
      "args": [
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "/usr/local/bin"
      }
    }
  ],
  "version": "2.0.0"
}